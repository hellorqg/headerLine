import Vue from 'vue'
import { button, Icon, NavBar, Cell, CellGroup, Field, Toast, CountDown } from 'vant'
Vue.use(button)
  .use(Icon)
  .use(NavBar)
  .use(Cell)
  .use(CellGroup)
  .use(Field)
  .use(Toast)
  .use(CountDown)
